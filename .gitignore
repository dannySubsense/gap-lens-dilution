venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.mypy_cache/

# Environment / secrets
.env
.env.local
.envrc
frontend/.env.local

# Local agent config (LORE identity, MCP secrets, not shared across hosts)
CLAUDE.md
CLAUDE.md.example
.claude/

# Frontend build artifacts
frontend/node_modules/
frontend/.next/
frontend/tsconfig.tsbuildinfo
.next/

# Development docs, specs, and reference notes (local working docs, not shipped)
docs/
specs/
archive/
ASK_EDGAR_MENTAL_MODEL.md

# Test artifacts (tests/ is tracked — it's the mandatory Playwright QC gate)
test-results/
frontend/test-results/

# Reference screenshots
screenshots/

# Local-only AskEdgar doc-drift scraper — canonical generalized version lives in
# the standalone api-doc-scraper repo. This copy is kept usable here for AskEdgar
# gap-checks but is never committed.
scripts/scrape_askedgar_reference.py
